# 版本记录

本项目遵循语义化版本。公开版从 `1.0.0` 开始编号，不映射任何非公开版本历史。

## 1.3.0 - 2026-08-19

- 同步脱敏后的最新工程能力基线，公开版继续采用独立语义化版本序列。
- 收紧自动触发范围：仅匹配明确的 FastGPT、FastGPT 导出 JSON 或相关迁移/集成任务，排除其他平台的独立工作流。
- 新增 FG080-FG083 跨节点变量引用校验和 FG084 应用版本显示标签校验，同时保持持久化机器键稳定。
- 增加 `expected_behavior`、预期兜底、用例矛盾预检及 IPv4/MAC 数量隔离断言。
- 新增可复现随机用例、脱敏工作流版本差异、答案守卫、IP/MAC 校验和重试/兜底设计参考。
- 分享接口支持 `--share-only`，避免向只接受分享标识的接口发送空 `appId`。
- 发布包改用 `slim-production` 配置，并增加导入包冒烟测试。

## 1.2.0 - 2026-08-11

- 统一分享接口执行器与离线评测器的断言内核，避免同一测试用例在不同工具中产生不同结论。
- 增加 Unicode NFKC、常见连字符和连续空白规范化，并支持精确标识符的严格匹配模式。
- 扩展节点、运行异常、最小/最大长度、最小节点数和最大时延等断言契约。
- 最大时延已声明但结果缺少耗时、或耗时超过阈值时，明确计入运行异常与首次失败。
- 增加从环境变量读取应用标识，减少硬编码和命令历史暴露。
- 增加 `restricted` 原始证据与 `shareable` 脱敏摘要的分级测试交付。
- 强化首次失败保留、复测隔离、测试编号脱敏和证据保留期限要求。

## 1.1.0 - 2026-08-11

- 展示名统一为“FastGPT工作流生成助手”，机器名保持 `fastgpt-workflow-assistant` 不变。
- 从偏 RAG 的助手扩展为多模式工作流助手，覆盖确定性流程、单模型、工具查询、审批写回、多工具 Agent、事件/批量及混合编排。
- 增加非 RAG 工作流工程指南和模式蓝图；知识库改为按需能力，不再是默认前置条件。
- 增加机器可读版本文件、版本一致性检查、远程发布清单比较、显式更新与回滚说明。
- 新增仓库根目录 `latest.json`、固定最新版 Release 附件命名和发布操作手册。
- 同步更新测试矩阵、自测试、发布清单和脱敏检查。

## 1.0.0 - 2026-08-10

- 首次公开发布。
- 提供 FastGPT 工作流候选 JSON 模板生成与结构校验。
- 提供知识库审计、Round-trip 差异比较、测试用例生成和结果评测。
- 提供分享接口低频回归与独立压力采样脚本。
- 提供工作流生命周期、安全门禁、发布清单和浏览器辅助测试方法。
- 完成公开版命名、样例和文档脱敏。
