# 贡献指南

感谢你帮助完善 FastGPT工作流助手。项目优先接受能够提升跨版本兼容性、导入成功率、知识检索质量、测试可重复性和安全性的改进。

## 选择协作渠道

- 使用问题、经验分享和尚未成形的想法：GitHub Discussions。
- 可复现缺陷、版本兼容问题和明确功能需求：GitHub Issues。
- 已验证的修改：Pull Request。
- 未公开的安全问题：GitHub Private vulnerability reporting，不要创建公开 Issue。

提交前请先搜索已有 Issues 和 Discussions，避免重复。

## 提交 Issue

尽量提供：

- FastGPT 版本与部署方式；
- 使用的 Skill 版本；
- 最小复现步骤和预期/实际结果；
- 已脱敏的 JSON、错误文本或执行轨迹；
- 问题发生在离线校验、导入、保存、回读还是运行阶段。

不要提交凭据、Cookie、真实业务数据、内部系统名称或无权公开的工作流导出物。

## 提交 Pull Request

1. 从主分支创建一个范围单一的分支。
2. 修改 Skill 规则时，说明触发场景、现有行为和预期行为。
3. 修改工作流模板时，说明目标 FastGPT 版本、结构来源、导入状态和需要重绑的资源。
4. 修改脚本时，增加或更新对应自测试。
5. 更新相关文档与版本记录。
6. 运行：

```powershell
python skill/fastgpt-workflow-assistant/scripts/test_skill.py
```

7. 在 PR 中附上测试输出摘要。需要目标平台验证但无法完成时，明确标记为 `pending`，不要宣称已经兼容。

## 设计原则

- 允许从版本化模板生成候选 JSON，不把“用户先导出最小工作流”设为前置条件。
- 目标实例的真实导入和执行结果优先于推测的 Schema。
- 确定性逻辑处理确定性问题，模型只处理必要的理解与表达。
- 结构校验、导入验证和运行回归是不同门禁。
- 首次失败是证据；复测不能覆盖首次结果。
- 通用能力进入 Skill，单一业务项目的实体、路由和诊断表留在项目层。

## 许可

除非你在提交时明确声明不作为贡献，否则有意提交并被项目接收的内容将按 Apache-2.0 许可。提交者应确保自己有权提供相关内容。

